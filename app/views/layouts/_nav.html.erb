<!-- vamos a renderizar separadamente la barra de navegación dependiendo si el usuario esta registrado o no -->
<% if !user_signed_in? %>
  <%= render '/layouts/nav_guest' %>
<% else %> 
  <%= render '/layouts/nav_user' %>
<% end %> 